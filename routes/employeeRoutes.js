// routes/employeeRoutes.js
/*const express = require('express');
const router = express.Router();
const EmployeeController = require('../controllers/EmployeeController');


router.get('/prueba',EmployeeController.prueba);   
/*router.post('/', EmployeeController.createEmployee);          // Crear empleado
router.get('/', EmployeeController.getEmployees);             // Obtener todos los empleados
router.get('/:id', EmployeeController.getEmployeeById);       // Obtener un empleado por ID
router.put('/:id', EmployeeController.updateEmployee);        // Actualizar un empleado
router.delete('/:id', EmployeeController.deleteEmployee);    // Eliminar un empleado

module.exports = router;*/ 
